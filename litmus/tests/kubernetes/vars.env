# the name of the node in which we're killing the docker service
DOCKER_SERVICE_KILL__TOTAL_CHAOS_DURATION=30 # in seconds
DOCKER_SERVICE_KILL__TARGET_NODE=litmus

# the name of the node in which we're killing the kubelet service
KUBELET_SERVICE_KILL__TOTAL_CHAOS_DURATION=30 # in seconds
KUBELET_SERVICE_KILL__TARGET_NODE=litmus

# node-cpu-hog
NODE_CPU_HOG__TOTAL_CHAOS_DURATION=30 # in seconds
NODE_CPU_HOG__NODE_CPU_CORE=
NODE_CPU_HOG__CPU_LOAD=
NODE_CPU_HOG__TARGET_NODE=litmus
